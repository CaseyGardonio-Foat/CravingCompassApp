import { BrowseDropdownDirective } from './browse-dropdown.directive';

describe('BrowseDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new BrowseDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
