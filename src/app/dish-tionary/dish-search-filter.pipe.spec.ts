import { DishSearchFilterPipe } from './dish-search-filter.pipe';

describe('DishSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DishSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
